{
    "cmd": "sudo  /opt/mesos-dns/mesos-dns-v0.4.0-linux-amd64 -config=/opt/mesos-dns/config.json",
    "cpus": 1.0,
    "mem": 1024,
    "id": "mesos-dns",
    "instances": 1,
    "constraints": [["hostname", "CLUSTER", "{{ mesos_hostname }}"]]
}